{
  "version": 2,
  "builds": [
    {
      "src": "bfash-main/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": ".output/public" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}
